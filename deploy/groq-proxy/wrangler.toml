name = "groq-proxy"
main = "worker.js"
compatibility_date = "2024-11-01"

# PROXY_SECRET задаётся как секрет (не хранить в файле):
#   wrangler secret put PROXY_SECRET
# Пусто/не задано — проверка X-Proxy-Secret отключена.
